services:
  - type: web
    name: discord-pro-bot
    env: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: WEB_ADMIN_PASSWORD
        sync: false
      - key: DATABASE_PATH
        value: /app/data/bot.sqlite3
